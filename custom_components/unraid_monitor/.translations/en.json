{
  "config": {
    "step": {
      "user": {
        "data": {
          "host": "Host",
          "port": "Port",
          "username": "Username",
          "password": "Password (leave blank if using SSH key)",
          "key": "SSH Private Key (paste the contents here)",
          "poll_interval": "Polling Interval (seconds)"
        },
        "description": "Set up your Unraid NAS connection. You can use either a password or an SSH key for authentication.",
        "title": "Unraid Monitor"
      }
    },
    "error": {
      "connection_failed": "Failed to connect. Please check your credentials."
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "poll_interval": "Polling Interval (seconds)"
        },
        "title": "Unraid Monitor Options"
      }
    }
  }
}
